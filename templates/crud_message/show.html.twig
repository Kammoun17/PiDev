{% extends 'base.html.twig' %}

{% block title %}Message{% endblock %}

{% block body %}
    <h1>Message</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdMessage</th>
                <td>{{ message.idMessage }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ message.message }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ message.date ? message.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crud_message_index') }}">back to list</a>

    <a href="{{ path('app_crud_message_edit', {'idMessage': message.idMessage}) }}">edit</a>

    {{ include('crud_message/_delete_form.html.twig') }}
{% endblock %}
